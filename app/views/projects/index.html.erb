  <h1>Proyectos: <%= @projects.size %></h1>
  <ol class="container">
    <% @projects.each do |project| %>
      <%= render partial: 'projects/show', locals: { project: project } %>
    <% end %>
  </ol>
  <%= link_to 'Nuevo Proyecto', new_project_path, class: 'new-button' %>
