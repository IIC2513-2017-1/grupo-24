<div id="rcorners1">
  <h3 class="project-title"><%=link_to project.title, mines ? user_project_path(current_user, project) : project_path(project) %> </h3>
  <span class="author-info">
    <strong><u> <%= mines ? "Publicado: #{project.publish ? 'Si' : 'No'}" : project.user.full_name %> </u></strong>
  </span>
</div>
